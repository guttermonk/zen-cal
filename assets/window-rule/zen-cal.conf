windowrule {
  name = zencal-rule
  float = on
  workspace = current, silent
  move = ((monitor_w*0.84)) ((monitor_h*0.03))
  size = ((monitor_w*0.1)) ((monitor_h*0.2))
  match:class = ^(TUI.zencal)$
}
